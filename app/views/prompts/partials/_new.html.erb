<div class="card-header">
  <h6 class="lead text-center pt-2">Onboard new clients easily using a custom fine-tuned AI model handled in a Ruby on Rails application.</h6>
</div>
<div class="row card-body m-4">
  <%= simple_form_for @prompt, url: prompts_path do |f| %>
    <%= f.input :input, as: :text, placeholder: I18n.t('views.prompt.form.placeholder'), label: false, input_html: { class: 'form-control form-control-md' } %>
    <%= f.button :submit, 'Generate', class: 'btn btn-light text-secondary border mt-2' %>
  <% end %>
</div>
