<div class="card-header">
  <h6 class="lead text-center pt-2">Onboard new clients with minimal effort using a custom fine-tuned AI model through a Ruby on Rails application</h6>
</div>
<div class="card-body m-5">
  <%= simple_form_for :prompt, url: prompts_path do |f| %>
    <%= f.input :text, as: :text, label: false, input_html: { class: 'form-control form-control-md' }, placeholder: t('prompt_service.form') %>
    <%= f.button :submit, 'Generate', class: 'btn btn-light text-secondary border mt-2' %>
  <% end %>
</div>